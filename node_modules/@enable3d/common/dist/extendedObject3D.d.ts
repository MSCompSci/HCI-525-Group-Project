/**
 * @author       Yannick Deubel (https://github.com/yandeu)
 * @copyright    Copyright (c) 2021 Yannick Deubel; Project Url: https://github.com/enable3d/enable3d
 * @license      {@link https://github.com/enable3d/enable3d/blob/master/LICENSE|GNU GPLv3}
 */
import { AnimationClip, AnimationMixer, Line, Mesh, Object3D, Points } from 'three';
import type PhysicsBody from './physicsBody';
import { AnimationAction } from 'three';
export interface ExtendedObject3D extends Line, Mesh, Points {
    isLine: any;
    isPoints: any;
    isMesh: any;
    type: any;
}
export declare class ExtendedObject3D extends Object3D {
    ptr: any | undefined;
    readonly isExtendedObject3D = true;
    readonly isGroup = false;
    private vector3;
    shape: string;
    name: string;
    body: PhysicsBody;
    hasBody: boolean;
    fragmentDepth: number;
    breakable: boolean;
    fractureImpulse: number;
    children: ExtendedObject3D[];
    parent: ExtendedObject3D | null;
    private _currentAnimation;
    private _animationActions;
    private _animationMixer;
    constructor();
    /** Returns all values relative to the world. */
    get world(): {
        theta: number;
        phi: number;
    };
    /** Get the theta relative to the world. */
    private get worldTheta();
    /** Get the phi relative to the world. */
    private get worldPhi();
    set animationMixer(animationMixer: AnimationMixer);
    get animationMixer(): AnimationMixer;
    /** Control your animations. */
    get anims(): {
        /** Get the name of the current animation. */
        current: string;
        /** Add animation name and the AnimationClip. */
        add: (name: string, animation: AnimationClip) => void;
        /** Get AnimationAction by animation name. */
        get: (name: string) => AnimationAction;
        /**
         * Play an animation.
         * @param name Animation name.
         * @param transitionDuration Transition duration in ms.
         * @param loop Should the animation loop?
         */
        play: (name: string, transitionDuration?: number, loop?: boolean) => void;
        /** Get the AnimationMixer */
        mixer: AnimationMixer;
    };
    /** @deprecated Please use anims instead! */
    get animation(): {
        /** Get the name of the current animation. */
        current: string;
        /** Add animation name and the AnimationClip. */
        add: (name: string, animation: AnimationClip) => void;
        /** Get AnimationAction by animation name. */
        get: (name: string) => AnimationAction;
        /**
         * Play an animation.
         * @param name Animation name.
         * @param transitionDuration Transition duration in ms.
         * @param loop Should the animation loop?
         */
        play: (name: string, transitionDuration?: number, loop?: boolean) => void;
        /** Get the AnimationMixer */
        mixer: AnimationMixer;
    };
    private _animsAdd;
    private _animsGet;
    private _animsPlay;
    /** @deprecated Use animation.play(name) instead! */
    setAction(name: string): void;
    traverse(callback: (object: ExtendedObject3D) => any): void;
    traverseVisible(callback: (object: ExtendedObject3D) => any): void;
    traverseAncestors(callback: (object: ExtendedObject3D) => any): void;
}
export default ExtendedObject3D;
//# sourceMappingURL=extendedObject3D.d.ts.map